<!DOCTYPE HTML>
<html xmlns:th="http://www.thymeleaf.org" lang="en">

<head>
	<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
	<title>Pizza</title>
</head>

<body class="bg-light text-dark">
	<div class="container text-center">
	<h1 class="mt-5" th:text="${pizza.name}"></h1>
	<img class="rounded border border-dark  border-top-0 border-right-0" th:src="${pizza.photo}" alt="Pizza" height="400px" weight="400px">
	<h4 class="mt-2" th:text="${pizza.description}"></h4>
	</div>
	
	<table class="container  table table-striped" id="table-saldi">
				<thead>
					<tr>
						<th>NOME</th>
						<th>DATA INIZIO</th>
						<th>DATA FINE</th>
					</tr>
				</thead>
				<tbody>
					<tr th:each="${pizza.saldi}">
						<td th:text="${saldi.nome}"></td>
						<td th:text="${pizza.description}"></td>
						<td th:text="${pizza.price} + ' €'"></td>
						<td><a class="btn btn-primary" th:href="@{|/pizze/pizza/${pizza.id}|}"><i class="bi bi-book"></i></a></td>
						<td><a class="btn btn-primary" th:href="@{|/pizze/update/${pizza.id}|}"><i class="bi bi-pencil"></i></a></td>
						<td>
							<form th:action="@{|/delete/${pizza.id}|}" method="post" th:data-confirm-delete=" 'Sei sicuro di voler cancellare la pizza ' + ${pizza.name} + '?'"
							 onclick="if(!confirm(this.getAttribute('data-confirm-delete'))) return false">
								<button class="btn btn-danger" type="submit" id="submitDeleteButton">
									<i class="bi bi-trash"></i></button>
							</form>
						</td>
					</tr>
				</tbody>
			</table>
	
</body>
</html>